events {}

http {
    server {
        listen 80;
        server_name localhost;   # или твой домен

        location / {
            proxy_pass http://pixel-nextjs:3000;   # "nextjs" – это имя сервиса в docker-compose
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}

; server {
;     listen 80;
;     server_name localhost:3000;
;
;     location /.well-known/acme-challenge/ {
;         root /var/www/certbot;
;     }
;
;     location / {
;         return 301 https://$host$request_uri;
;     }
; }
;
; server {
;     listen 443 ssl;
;     server_name localhost:3000;
;
;     ssl_certificate /etc/letsencrypt/live/localhost:3000/fullchain.pem;
;     ssl_certificate_key /etc/letsencrypt/live/localhost:3000/privkey.pem;
;     include /etc/letsencrypt/options-ssl-nginx.conf;
;     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
;
;     location / {
;         proxy_pass http://nextjs:3000;
;         proxy_set_header Host $host;
;         proxy_set_header X-Real-IP $remote_addr;
;         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
;         proxy_set_header X-Forwarded-Proto $scheme;
;     }
; }